# Copy this to .env and fill in real keys. Never commit .env itself.
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# The gateway's own admin key and its key/spend database (added in step 8).
LITELLM_MASTER_KEY=sk-onegateway-master
DATABASE_URL=postgresql://user:pass@localhost:5432/litellm
